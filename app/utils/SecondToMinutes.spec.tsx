import SecondToMinutes from './SecondToMinutes'

describe('Second To Minutes', () => {

it('should convert seconds to minutes', () => {
	expect(SecondToMinutes(60)).toBe('01:00')
	// expect(SecondToMinutes(90)).toBe('1:30')
	// expect(SecondToMinutes(120)).toBe('2:00')
	// expect(SecondToMinutes(150)).toBe('2:30')
	// expect(SecondToMinutes(180)).toBe('3:00')
})



})
